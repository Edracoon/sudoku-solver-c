/** Solves a sudoku puzzle.
 * @param flat 81-char string, row-major, '0' = empty cell, '1'-'9' = given
 * @returns solved 81-char string, or null if invalid/unsolvable
 */
export declare function solve(flat: string): string | null;
